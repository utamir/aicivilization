# Validation report: <mechanism or release>

- Commit / build id:
- Runner commands used (from `tools/`): 
- Type-check: `tsc -p tsconfig.sim.json` result
- Invariant tests: pass/fail list
- Presets × seeds run (at least baseline, one positive, one negative, seeds 7/11/23), horizon 200 years:
  - NaN or Infinity anywhere: yes/no (must be no)
  - Absurdity list (must all be no): reserve margin > 200%; the same named invention appearing twice; population flat to 0.1% for 30 years; a project of one kind committed more than once a year per civilization for a decade; expansion into sea or orbit while land pressure < 0.9; a milestone achieved before its requirements; a trajectory label contradicting population or output (growth with −40% people)
  - Reference numbers vs II.8, with deltas and one-line explanations
- New evidence entries added: ids
- Verdict: merge / return with the specific failing line
